{
  "watched_repos": [
    {
      "repo": "owner/repo-name",
      "trigger_labels": ["rfc", "bug"],
      "enabled": true,
      "description": "示例：监控某个项目的 RFC 和 Bug"
    }
  ],
  "settings": {
    "poll_interval_minutes": 5,
    "max_events_per_run": 50,
    "lookback_minutes": 10
  }
}
